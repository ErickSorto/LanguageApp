<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/tan_background">

    <LinearLayout
        android:id="@+id/layout_text_images"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_height"
        android:orientation="horizontal">
        <ImageView
            android:id="@+id/image"
            android:layout_width="@dimen/list_item_height"
            android:layout_height="@dimen/list_item_height"
            android:src="@mipmap/ic_launcher"/>

        <LinearLayout
            android:id="@+id/text_container"
            android:layout_width="match_parent"
            android:layout_height="88dp"
            android:orientation="vertical"
            android:paddingLeft="16dp"
            android:background="@color/category_numbers">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="0sp"
                android:id="@+id/miwok_text_view"
                android:text="@string/lutti"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                android:textColor="@android:color/white"
                android:layout_weight="1"
                android:gravity="bottom"
                android:textStyle="bold"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="0sp"
                android:layout_weight="1"
                android:gravity="top"
                android:id="@+id/default_text_view"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                android:textColor="@android:color/white"
                android:text="@string/one"/>
        </LinearLayout>
    </LinearLayout>

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/baseline_play_arrow_white_24dp"
        android:layout_centerInParent="true"
        android:layout_alignEnd="@+id/layout_text_images"
        android:layout_alignRight="@+id/layout_text_images"
        android:layout_marginRight="16dp"/>

</RelativeLayout>